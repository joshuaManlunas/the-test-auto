{
  "name": "playwright_framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pw:install": "npx playwright install",
    "pw:test:initial": "npx playwright test --grep=@initial --project=chromium --headed",
    "pw:test:nonPOM": "npx playwright test --grep=@NonPOM --project=chromium --headed",
    "pw:report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.22.0",
    "ts-node": "^10.6.0",
    "tslint": "^6.1.3",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "properties-reader": "^2.2.0",
    "winston": "^3.6.0"
  }
}
